# Software Engineering Project

- after downloading Flutter, run the command "flutter doctor" in the terminal to make sure you have everything and it's working as expected
- lib folder and pubspec.yaml are the only things that should be modified
